apiVersion: v1
kind: Secret
metadata:
  name: gopro-processor-secrets
  namespace: gopro-processor
  labels:
    app.kubernetes.io/name: gopro-video-processor
    app.kubernetes.io/component: secret
type: Opaque
data:
  # Base64 encoded secrets
  # Use: echo -n 'your-secret' | base64
  redis-password: ""  # Optional Redis password
  session-secret: "Z29wcm8tc2VjcmV0LWtleS0yMDI1"  # gopro-secret-key-2025
stringData:
  # Plain text secrets (will be base64 encoded automatically)
  app-secret: "gopro-video-processor-secret-2025"
---